# Tracer implementation progress

Plan: [tracer 0.1](tracer-0.1.md). Starting source: `21effb2`. Branch: `codex/tracer-0.1`, isolated in `.worktrees/tracer`.

User authorized implementation and orchestration. Historical planning-only stop instructions are superseded; original `plans/` files remain preserved. No later milestone is authorized by this tracer run.

## Status

- TR-01 harness complete and independently reviewed. Integrated source through `58de407`; four unit tests, two MCP tests, TypeScript check/build, native build and CTest passed in coordinator worktree. Review/report retained in `docs/reviews/tr01-harness.md` and `tr01-report.md`.
- TR-02 starting: bounded actual WebGPU/native/FFGL feasibility experiment. Full integration readiness remains unavailable until its measurements exist.
- TR-03–07 pending prerequisite gates. No GPU transport, host integration or performance result is claimed.

## Decisions and checkpoints

- Coordinator owns integration, shared contracts and this log. Bounded agents use exclusive paths/worktrees; hardware runs are serialized.
- Baseline is documentation only; no pre-existing application tests can run.
- Ruling: skill task extractor expects `Task N` headings, whereas the approved plan uses `TR-NN`; extract those sections without rewriting the approved plan.
- One read-only specialist verifies dependency/API choices while environment/harness work proceeds.
- Checkpoints: `0024062` actual host/toolchain observations; `77bbf70` actual model-visible MCP fixture and pinned Electron/Chromium readiness audit. Host reports RTX 2070; renderer/bridge LUID matching remains unproved.
- Ruling: TR-01 toolchain/profile checks permit the bounded TR-02 experiment that discovers actual renderer/bridge LUIDs and transport capability. Full preflight integration readiness remains false until those measurements exist; it cannot be a circular prerequisite for collecting them. No SDK/editor expansion before TR-02 passes.
- All three TR-01 review findings closed: MCP aggregate timeout, structured missing-Electron handling, and synthetic test evidence isolation. Original plans remain unchanged.
- TR-02 ruling: select the bridge design's bounded fallback 1, an owned three-slot D3D/GL `TextureTransport`, with worker-side NV interop and explicit GPU readiness/retirement. Spout convenience receive has unproved driver waits. Electron/WebGPU and the authoring contract remain unchanged; Spout compatibility is not claimed. Actual shared-context and same-adapter behavior still must pass the experiment.
- TR-02 partial checkpoint `ce04c24` on `codex/tr02-gpu`: native pool tests and N-API loader correction; sandboxed worker raw WebGPU → Electron shared BGRA → native D3D import/copy/EVENT completion observed. Fifteen-second probe reported 869 paint events and zero retained textures at end; bridge identified NVIDIA LUID `0:71045`. Output height was clamped to 1020, and Three.js/FFGL paths are not yet proved. This checkpoint does not pass TR-02.
- Native-only `Lux TR02 Probe` DLL staged in the existing Extra Effects folder, SHA256 `505782a9bd82215c60d17e88d1e2834aaef295e96035e032eba94661b94697aa`. It tests shared GL worker context; it is not the browser frame receiver. Desktop automation could not reliably apply input with the clipped host capture, so user assistance to load/trigger the probe was requested asynchronously. Transport implementation continues; no host context result yet.
- Actual Resolume baseline confirmed by user and PID30464 log: expected moving diagnostic pattern, sharedContext=true, nvInterop=true on RTX2070; callbacks advancing. Evidence: evidence/tracer-0.1/tr02-resolume-baseline. Browser texture import and shutdown remain under investigation; TR-02 is not complete.
- INCIDENT: user reports whole-computer failure requiring restart during native baseline experiment. Hardware runs paused; TR-02 blocked on failure investigation. Windows unclean-shutdown events and post-reboot probe log saved in evidence/tracer-0.1/tr02-system-incident. Cause unconfirmed; previous context success is not stability acceptance.
- Offline incident review: staged probe disabled with verified hash; current receiver has unbounded worker join and exception cleanup gaps. Exact earlier baseline source not recovered, so fixes cannot yet be attributed to the crash. See docs/reviews/tr02-incident-offline.md. Test activity overlap recorded; future runs require exclusive supervision and attributable builds.
- Offline repairs active in isolated lifecycle and experiment-runner worktrees. Producer lease/drain repair committed on codex/tr02-gpu through a843d3e, independently reviewed, four CPU tests pass. Ambiguous native acceptance retains textures and prevents false successful cleanup. Native shutdown race reproduced in a CPU test; repairs and runner validation continue without GPU execution.
- Combined repaired branch now through 0cb28c5 on codex/tr02-gpu: Release build, 18 unit tests, 5 native CPU tests, 2 MCP profile tests, typecheck and syntax emission passed. First reviewed exclusive standalone run f7193eb4 failed promptly during shared-context initialization; job confirmed cleanupComplete=true and no timeout. Resolume remained closed. Exact context creation/activation instrumentation is next; TR-02 remains unpassed.
- User explicitly requested other parts in parallel. Ruling: supersede strict task sequencing for independent implementation only; GPU/host/integration acceptance remains gated. Compiler+SDK lane owns .worktrees/tr03-compiler, Studio shell lane owns .worktrees/tr05-studio-shell, native lane remains .worktrees/tr02-lifecycle. Canonical shared compiler/control DTOs committed2d9984e, verified with two CPU tests/typecheck/build. Exact dependency pins committed353a99c. Neither compiler nor Studio may claim connected runtime or completed tracer before real integration.
- Parallel checkpoint: compiler emits real JS/source maps and deterministic hashes in first CPU integration; final compiler tests pending. Studio has a disconnected React shell and presentation-only window controls with nine CPU tests passing; semantic/build verification pending. Native host-created context handoff committed 89ee249 with seven CPU tests and full native build passing; coordinator review requested an additional pre-try exception/ownership audit before hardware. No new GPU/Resolume acceptance. Matching @types/three 0.186.0 is centrally pinned for real SDK type checking.
- User stack requirement: Studio uses React, Material UI as its basic design system, and RTL (React Testing Library) for interaction tests. Studio agent is applying themed MUI controls and user-facing DOM tests; preview-first layout remains. Central pins use MUI 9.4.0, Emotion 11, RTL 16.3.3, and jsdom 26.1.0 compatible with pinned Node 24.12.0. Completion awaits agent build/typecheck/test evidence.
- Integration checkpoint: GPU source/evidence merged in e7ac98c; combined typecheck/build and 21 CPU unit tests passed. Context-only run61f7426b passed; paired runa618071e safely exited but failed receiver attachment, now under monotonic-poll investigation. Compiler checkpoints706cf47/f2b9237 integrated pending diagnostic-budget P2 correction. Studio919a993 awaiting busy-state P2 correction before integration. Runtime clock/seed569e9bf independently reviewed and seven tests plus typecheck passed. No TR-02/03/05 completion claimed.
- User-approved scope pivot: standalone Lux author/view/save and AI image iteration now; Resolume/export deferred. Rendering/control/capture checkpoint81dccce passed actual Electron smoke. Save/open and authenticated standalone MCP loop now pass, including changed image bytes, stale edits and hanging-candidate rejection while retaining the previous preview. See standalone-lux.md and evidence/standalone-lux. Native experiments parked; no original TR-02/07 completion claimed. Agent capacity failures required coordinator implementation and self-review of the standalone integration.
- QA follow-up: user confirmed saved-file reopen. Implemented preview-only fullscreen with fading Escape notice, authoritative native transition state, and first-Escape exit. Removed routine command-success alerts and separated intensity writes from transport busy state. All 25 Studio CPU tests and Studio semantic typecheck pass. Interactive verification is pending: user closed Studio for a separate agent's transport test and explicitly prohibited relaunch until their OK. No Studio/GPU launch performed for these fixes.
- QA follow-up: user reports the prior fullscreen/control fixes look good; remaining playback label flashed Sending request during transitions. Removed that transient label so the last applied playback status remains until runtime confirmation. Regression reproduced before the fix; all 25 CPU tests and Studio typecheck pass afterward. Built update awaits next Studio launch; current QA session was not interrupted.
